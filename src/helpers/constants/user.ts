const USER_ROLES = {
	admin: 'admin',
	user: 'user',
	guest: 'guest'
} as const;

const USER_ID_CNAME = '_pcscuicn';
const SITE_ID_CNAME = '_pcscsicn';

export { USER_ROLES, USER_ID_CNAME, SITE_ID_CNAME };
